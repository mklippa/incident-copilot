[project]
name = "incident-copilot"
version = "0.1.0"
description = "Incident Copilot — agentic support/incident triage demo project"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "mcp>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
