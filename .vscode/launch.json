{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "classify_loop",
      "type": "debugpy",
      "request": "launch",
      "module": "incident_copilot.classify_loop",
      "cwd": "${workspaceFolder}",
      "python": "${workspaceFolder}/.venv/bin/python",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
